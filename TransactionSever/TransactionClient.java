import java.io.*;

public class TransactionClient
{
    public TransactionClient()
    {
        private TransactionServerProxy serverProxy;

        serverProxy = new TransactionServerProxy();
    }
}